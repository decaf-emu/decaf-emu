#pragma once
#include "types.h"

namespace latte
{

enum SPI_BARYC_CNTL
{
   SPI_CENTROIDS_ONLY          = 0,
   SPI_CENTERS_ONLY            = 1,
   SPI_CENTROIDS_AND_CENTERS   = 2,
};

} // namespace latte
